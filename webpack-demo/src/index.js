import './style.css';

const front = document.querySelector(".create");
const dropdown = document.querySelector('.dropdown');

front.addEventListener('click', toggleDropdown);

function toggleDropdown() {
  
}